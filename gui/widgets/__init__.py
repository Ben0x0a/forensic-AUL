"""Reusable GUI widgets and the logging→GUI bridge.

Defines : the `widgets` subpackage (custom Qt widgets + the log handler).
Used by : gui.views (custom widgets registered on QUiLoader) and
          gui.controllers / gui.app (logging sink).
Uses    : PySide6.
"""
